import config from "<%= mailingConfigPath %>";
import sendMail from "<%= relativePathToEmailsDir %>";

// template imports
<%= templateImports.join("\n") %>

// preview imports
<%= previewImports.join("\n") %>

const previews = { <%= previewConsts.join(", ") %> };
const templates = { <%= templateModuleNames.join(", ") %> };
const bundleId = <%= bundleId %>;

export { sendMail, config, templates, previews, bundleId };
const moduleManifest = { sendMail, templates, previews, bundleId };
export default moduleManifest;
